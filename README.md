# oss_work

오픈소스소프트웨어 개인과제

1 .버전관리 개요 정리
02. 1-2 버전관리 개요
1) 버전 관리의 필요성
파일 작업을 하다 보면 과거 어느 시점의 상태로 되돌아가고 싶을 때가 있다.
예전에는 파일 이름을 바꿔가며 여러 버전을 저장하거나, 아예 폴더를 복사해서 관리했지만, 파일이 많아질수록 비효율적이다.
버전 관리는 이러한 문제를 해결하기 위해 파일의 변경 이력 전체를 체계적으로 기록하고 추적하는 기능을 제공한다.
2) 버전 관리란?
시간이 흐르면서 파일이 어떻게 달라졌는지 기록하는 시스템
누가, 언제, 어디를, 어떻게 수정했는지 확인 가능
필요한 경우 특정 시점으로 ‘시간여행’처럼 되돌아갈 수 있음
여러 사용자가 동시에 작업할 때 충돌을 해결하는 기반 제공
3) 버전관리 도구의 방식
CLI(Command Line Interface): Git Bash처럼 텍스트 기반으로 명령을 입력
GUI(Graphical User Interface): 소스트리 등 그래픽 화면으로 직관적 사용 가능
4) 커밋(commit) 개념
현재 프로젝트 상태를 저장하는 ‘스냅샷’
파일의 변경 이력을 저장소에 기록하는 행위
커밋이 순서대로 쌓이며 버전 히스토리가 형성됨
가장 최근 커밋을 가리키는 포인터 = HEAD
5) 지역 저장소와 원격 저장소
지역(Local) 저장소: 내 컴퓨터 안의 저장소
원격(Remote) 저장소: 깃허브 같은 서버 기반 저장소
관련 명령어
clone: 원격 저장소 복제
add: 변경사항 스테이징
commit: 스냅샷 저장
push: 지역 → 원격
pull: 원격 → 지역

2. 깃과 깃허브 개요 정리
03. 1-3 깃과 깃허브 개요
1) Git의 탄생과 특징
Git은 2005년 리누스 토르발스가 개발한 분산 버전 관리 시스템이다.
모든 개발자는 자신의 PC에 전체 소스 코드 복사본을 가지고 있어 빠르고 안정적이다.
2) Git의 주요 기능
파일의 변경을 시간순으로 추적
여러 개발자가 독립적으로 작업 후 합치기(merge) 가능
브랜치를 활용한 비선형 개발 가능(여러 버전 동시에 개발)
3) Git의 내부 구조
Working Directory(작업 폴더): 실제 작업하는 공간
Staging Area: 커밋할 변경 사항을 모아두는 공간
Repository(.git): 모든 버전 기록이 저장되는 공간
Stash: 잠깐 저장하는 임시 공간
add → commit → push 구조로 상태가 이동한다.
4) 브랜치 개념
‘나무 가지’처럼 기존 코드를 복사해 독립적으로 개발하면서도
필요하면 다시 메인 브랜치와 merge할 수 있는 방식
새로운 기능 개발, 실험용 작업을 안전하게 수행할 수 있음
5) GitHub란?
Git 저장소를 온라인에서 관리할 수 있는 플랫폼
협업, 코드 리뷰, 문서화 등 다양한 기능 제공
무료 계정은 공개 저장소 기본 제공, 비공개는 유료
전 세계 개발자들이 함께 사용하는 대표적인 오픈소스 플랫폼

4. 깃 설정과 저장소 생성 정리
1) Git 최초 설정
새 PC에서 Git을 사용하기 전에 딱 한 번 해두는 설정들이 있다.
필수 6가지 설정
git config --global user.name "사용자이름"
git config --global user.email "이메일"
git config --global core.autocrlf true       # 줄바꿈 자동변환(Windows)
git config --global core.safecrlf false      # 줄바꿈 오류 방지
git config --global core.editor "code --wait"  # 기본 편집기
git config --global init.defaultBranch main    # 기본 브랜치 이름
모두 전역 설정 파일(~/.gitconfig)에 저장되어 이후 다시 설정할 필요 없다.
2) Git 저장소 생성
프로젝트 폴더를 Git 저장소로 만들기 위해 git init 사용한다.
두 가지 방식
git init : 현재 폴더를 저장소로 변경
git init 폴더명 : 새로운 폴더를 만들고 저장소 생성
생성을 마치면 .git 폴더가 자동으로 생성되며, 여기에 모든 버전 기록이 저장된다.
.git 폴더를 삭제하면 저장소 기능이 사라지므로 주의해야 한다.
3) 실습 예시 흐름
Git 설정 6가지 입력
작업 폴더 생성
Git Bash 실행
git init으로 저장소 생성
ls -al로 .git 폴더 존재 확인

06. 비주얼 스튜디오 코드와 리눅스 명령 – 정리
비주얼 스튜디오 코드는 MS에서 만든 가볍고 확장성이 뛰어난 오픈소스 에디터로, 다양한 언어와 확장 기능을 지원한다. 설치 시 탐색기에서 “Code로 열기” 기능을 활성화해 두면 작업 효율이 높아진다.
실행 화면은 활동바, 사이드바, 편집기, 상태바로 구성되며 직관적인 UI를 제공한다.
리눅스 기본 명령으로는 pwd, ls, cat, echo, cp, rm, mv 등이 있다.
특히 리다이렉션(>, >>)과 파이프(|)는 파일 생성 및 출력 전달에 자주 활용된다.
VSCode와 리눅스 명령을 함께 사용하면 개발 환경을 더 효율적으로 구축할 수 있다.

07. 깃 커밋과 로그 – 정리
Git은 작업 디렉토리(Working Directory), 스테이징 영역(Staging Area), 저장소(Repository) 세 영역으로 구성된다.
버전 관리를 위해서는 git add로 스테이징 후 git commit을 통해 저장소에 기록해야 한다.
처음 생성된 파일은 untracked 상태이며, git status를 통해 상태를 확인할 수 있다.
커밋 메시지는 -m 옵션을 사용하거나 기본 편집기를 통해 입력한다.
버전 이력은 git log, git log --oneline, git show 등을 활용해 조회할 수 있으며, 커밋의 변화 내용까지 함께 확인할 수 있다.
이 단계를 통해 Git의 가장 기본적인 버전 관리 흐름을 익힐 수 있다.

08. 로그 이력과 과거 여행 – 정리
여러 번의 커밋을 진행한 후 로그 이력을 통해 버전 변화를 확인할 수 있다.
git log, git log --graph, git log --reverse 등 다양한 옵션을 활용하면 커밋 간 관계나 흐름을 시각적으로 이해하는 데 도움이 된다.
Git은 HEAD 포인터를 이용해 현재 커밋 위치를 나타내며, HEAD~, HEAD~2, HEAD^ 등 표기법으로 이전 커밋을 가리킬 수 있다.
그리고 git checkout HEAD~1처럼 특정 시점의 버전으로 이동할 수 있다.
이 과정은 Git이 단순히 파일을 저장하는 도구가 아니라 시간 여행하듯 특정 시점으로 되돌아갈 수 있는 강력한 버전 관리 시스템이라는 사실을 보여준다.

09. [실습] 커밋과 로그 이력, 과거 여행 – 정리
실습에서는 실제로 hello.txt 파일을 생성하여 수정 → 추가 → 커밋을 반복하며 Git의 버전 관리 흐름을 체험한다.
총 3회의 커밋을 만든 뒤, 각 커밋이 저장하는 스냅샷의 차이를 git log, git show를 통해 확인한다.
또한 git checkout HEAD~, git checkout HEAD~2 명령을 이용해 과거 버전으로 이동해 보고 다시 git checkout main으로 최신 버전으로 돌아오는 실습을 수행한다.
이를 통해 “작업 디렉토리·스테이징·저장소”가 어떻게 변하는지 시각적으로 이해할 수 있었다.
Git의 상태가 깨끗해야(checkout clean) 과거로 이동할 수 있으며, 필요할 경우 git stash를 통해 임시로 작업 내용을 저장할 수도 있음을 배웠다.

10. 파일 비교(diff) – 정리
Git의 diff 명령은 파일의 변경 내용을 비교하여 어떤 라인이 추가되었는지, 삭제되었는지를 확인할 수 있는 도구이다.
여러 커밋 사이의 차이를 비교할 수도 있고, 현재 작업 디렉토리와 스테이징 영역의 차이도 비교할 수 있다.
주요 명령어는 다음과 같다.
git diff : 작업 디렉토리 ↔ 스테이징 비교
git diff --staged : 스테이징 ↔ 저장소 비교
git diff A B : 두 커밋 간 차이 비교
diff는 버그 추적, 코드 리뷰, 기능 수정 시에 매우 중요한 기능이며 Git의 핵심 기능 중 하나다.

11. 파일 삭제(rm)과 복원(restore) – 정리
Git에서 파일을 삭제하는 방법은 리눅스 명령어 rm과 Git 명령 git rm으로 나뉜다.
rm은 작업 디렉토리에서만 파일을 삭제하고, git rm은 작업 디렉토리 + 스테이징 영역에서 동시에 파일을 제거한다.
또한 git rm --cached를 사용하면 스테이징 영역에서만 파일을 삭제하고 실제 파일은 유지된다.
삭제된 파일은 커밋하면 Git 저장소에서도 기록이 제거되지만, 필요할 경우 git restore 명령을 통해 작업 디렉토리나 스테이징 영역, 또는 두 곳 모두를 저장소 상태로 되돌릴 수 있다.
이를 통해 실수로 삭제한 파일도 쉽게 복원할 수 있다는 점을 배웠다.

12. [실습] 파일 diff, rm, restore – 정리
이 실습에서는 Git의 세 영역(작업 디렉토리, 스테이징, 저장소)을 기준으로 파일이 어떻게 달라지는지 직접 비교해보았다.
git diff, git diff --staged, git diff HEAD를 통해 각 상태 간의 차이를 확인할 수 있었다.
또한 파일을 삭제한 후 git rm, git rm --cached를 사용해 영역별 삭제 동작을 실습했고, 이후 커밋 시 로그에 삭제 내역이 반영되는 것을 확인했다.
마지막으로 git restore로 작업 디렉토리나 스테이징 상태를 저장소와 동일하게 되돌리며, Git이 단순 저장이 아니라 되돌리기까지 포함하는 버전 관리 시스템임을 이해하게 되었다.

13. 버전과 태그 활용 – 정리
소프트웨어 버전은 보통 major.minor.patch 방식(SemVer)으로 관리된다.
버전을 구분하기 위해 Git에서는 태그(tag) 기능을 사용하며, 특정 커밋에 이름을 붙여 버전처럼 활용할 수 있다.
태그는 두 가지 종류가 있다.
주석 태그(Annotated Tag): 작성자, 날짜, 메시지 등 정보 포함
일반 태그(Lightweight Tag): 이름만 존재하는 간단한 태그
git tag, git show, git tag -d 등을 통해 태그를 생성·조회·삭제할 수 있으며, 이를 통해 중요한 버전이나 특정 시점을 명확히 기록할 수 있다는 점이 유용했다.

14. 브랜치 개요와 관리 – 정리
브랜치는 Git에서 독립적으로 작업을 진행하기 위한 ‘작업 라인’ 역할을 한다.
기본 브랜치는 main이며, 새로운 기능 개발이나 테스트를 위해 별도의 브랜치를 만들어 사용할 수 있다.
주요 명령은 다음과 같다:
git branch: 브랜치 목록 확인
git branch 새브랜치: 브랜치 생성
git switch 브랜치, git checkout 브랜치: 이동
git merge: 브랜치 병합
브랜치를 사용하면 메인 코드에 영향을 주지 않고 실험할 수 있어 협업과 소프트웨어 개발 흐름이 훨씬 안정적이 된다는 것을 알게 되었다.

15. [실습] 브랜치 개요와 관리 – 정리
실습에서는 브랜치를 직접 생성하고 이동하며, 브랜치별로 다른 내용을 담은 파일을 커밋해 보았다.
이를 통해 브랜치마다 독립된 작업 환경이 유지된다는 것을 확인했다.
또한 git merge를 통해 브랜치 내용을 main 브랜치에 병합하는 과정도 진행했다.
충돌(conflict)이 발생할 수 있다는 점도 실습에서 다루었고, Git이 충돌 내용을 표시해 개발자가 직접 해결하도록 돕는 구조라는 점도 이해하였다.
브랜치를 적절히 사용하면 프로젝트를 안전하고 구조적으로 관리할 수 있다는 것이 가장 큰 배움이었다.

16. 원격 저장소 복제(Clone) – 정리
GitHub 같은 원격 저장소는 인터넷에 저장된 버전 관리 공간이다.
Clone은 이 원격 저장소 전체를 내 컴퓨터(지역 저장소)로 그대로 복사해 오는 과정이다.
GitHub에서 저장소를 만들고 주소를 복사한다.
명령어 git clone 저장소주소를 실행하면 같은 이름의 폴더가 생성되며 코드가 내려온다.
git clone URL 폴더명처럼 새로운 이름으로 복제할 수도 있다.
Clone을 하면 원격 저장소 별칭(origin)이 자동으로 설정되며,
git remote -v로 이를 확인할 수 있다.
VSCode에서도 Clone Git Repository 기능을 사용하여 쉽게 복제할 수 있다.
유명 OSS(예: VSCode, Atom, TensorFlow 등)도 동일한 방식으로 Clone 가능하다.

17. 지역과 원격 저장소 연동 – Push & Pull – 정리
지역 저장소에서 작업한 내용을 원격 저장소로 올리거나(pushing),
반대로 원격 저장소 변경 사항을 가져오는(pulling) 과정이 매우 중요하다.
핵심 개념
push: 내 컴퓨터에서 수정한 내용을 GitHub로 업로드
→ git push origin main
pull: GitHub에 있는 최신 내용을 내 컴퓨터로 가져오기
→ git pull origin main
origin은 원격 저장소 기본 별칭
push 전에는 반드시 파일을 add → commit 해야 원격 저장소로 업로드 가능
중요 포인트
Pull을 먼저 하지 않으면 충돌(conflict)이 발생할 수 있다.
특히 여러 사람이 함께 작업할 때는 pull → 작업 → commit → push 흐름이 필수적이다.

18. [실습] 지역과 원격 저장소 브랜치 연동 – 정리
이 실습에서는 원격 저장소와 지역 저장소 간의 브랜치 연결 과정을 중심으로 학습했다.
주요 실습 내용
Clone된 저장소는 기본적으로 main 브랜치를 따라간다.
새로운 브랜치를 만들어 작업한 뒤,
그 브랜치를 원격 저장소에도 동일하게 생성(push)할 수 있다.
git push -u origin 새브랜치
→ 지역 브랜치를 원격 브랜치와 연결(upstream 설정)
이후에는 단순히 git push만 입력해도 자동으로 같은 브랜치로 push됨.
VSCode에서도 브랜치 생성, 전환, push/pull 등 관리가 가능했다.




